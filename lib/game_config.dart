import 'package:flame/components.dart';

class GameConfig {
  static const rows = 19;
  static const columns = 12;
  static const double sizeAsset = 384;
  static const double sizeCell = 27;

  static const double speed = 110;
  static Vector2 right = Vector2(speed, 0);
}
